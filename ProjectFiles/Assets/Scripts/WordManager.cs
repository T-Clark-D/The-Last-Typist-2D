using UnityEngine;
using System.Collections;

public class WordManager : MonoBehaviour {
    public WordVomit WVomit;

    // Use this for initialization
    void Start () {
        WVomit = new WordVomit();
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
